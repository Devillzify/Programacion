package Principiante;

public class Arrays_ordenar
{
    public static void main(String[] args)
    {
        int[] numeros ={4,2,10,6,9,3};
        int numerobajo=0;
        int primero = numeros[0];

        for(int i = 0; i < numeros.length ; i++)  // comparo todos los numeros  numero[0]es menor que numero[1] entonces numerobajo = numero[1]
        {
            if(numeros[0] > numeros[i])
            {
                System.out.println(numeros[i]);
            }
            for(int j = 1; j < numeros.length; j++)
            {

            }


        }
    }
}

